<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/fixi_primary</item>
        <item name="colorPrimaryDark">@color/fixi_primary_dark</item>
        <item name="colorAccent">@color/fixi_accent</item>
        <item name="android:statusBarColor">@color/fixi_primary_dark</item>
        <item name="android:navigationBarColor">@color/fixi_primary_dark</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@color/fixi_surface</item>
        <item name="android:statusBarColor">@color/fixi_primary_dark</item>
        <item name="android:navigationBarColor">@color/fixi_primary_dark</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/fixi_splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/fixi_splash_background</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>
